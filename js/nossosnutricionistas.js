$(document).ready(function () {


    $('#butaocancelarmsg1').click(function () {
        var m1 = $('#msg1');
        m1.find('input[type = "text"]').val('');
        m1.find('input[type = "email"]').val('');
        m1.find('textarea').val('');
    });

    $('#butaocancelarmsg2').click(function () {
        var m2 = $('#msg2');
        m2.find('input[type = "text"]').val('');
        m2.find('input[type = "email"]').val('');
        m2.find('textarea').val('');
    });

    $('#butaocancelarmsg3').click(function () {
        var m3 = $('#msg3');
        m3.find('input[type = "text"]').val('');
        m3.find('input[type = "email"]').val('');
        m3.find('textarea').val('');
    });

    $('#butaocancelarmsg4').click(function () {
        var m4 = $('#msg4');
        m4.find('input[type = "text"]').val('');
        m4.find('input[type = "email"]').val('');
        m4.find('textarea').val('');
    });

    $('#butaoenviarmsg1').click(function () {
        alert('Mensagem enviada com sucesso. Iremos responder o mais brevemente possível. Por favor esteja atento à sua caixa de entrada do correio eletrónico. Obrigado.');
        var m1 = $('#msg1');
        m1.find('input[type = "text"]').val('');
        m1.find('input[type = "email"]').val('');
        m1.find('textarea').val('');
    });

    $('#butaoenviarmsg2').click(function () {
        alert('Mensagem enviada com sucesso. Iremos responder o mais brevemente possível. Por favor esteja atento à sua caixa de entrada do correio eletrónico. Obrigado.');
        var m2 = $('#msg2');
        m2.find('input[type = "text"]').val('');
        m2.find('input[type = "email"]').val('');
        m2.find('textarea').val('');
    });

    $('#butaoenviarmsg3').click(function () {
        alert('Mensagem enviada com sucesso. Iremos responder o mais brevemente possível. Por favor esteja atento à sua caixa de entrada do correio eletrónico. Obrigado.');
        var m3 = $('#msg3');
        m3.find('input[type = "text"]').val('');
        m3.find('input[type = "email"]').val('');
        m3.find('textarea').val('');
    });

    $('#butaoenviarmsg4').click(function () {
        alert('Mensagem enviada com sucesso. Iremos responder o mais brevemente possível. Por favor esteja atento à sua caixa de entrada do correio eletrónico. Obrigado.');
        var m4 = $('#msg4');
        m4.find('input[type = "text"]').val('');
        m4.find('input[type = "email"]').val('');
        m4.find('textarea').val('');
    });




});